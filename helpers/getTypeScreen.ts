// import { TypeScreen } from '~/types/base/screen';
// import screens from '~/plugins/constants/screens';

// const getTypeScreen = (pxScreen: number): TypeScreen => {
const getTypeScreen = (): void => {
  // const { tablet, desktop } = screens;
  // if (pxScreen < tablet) return 'mobile';
  // if (pxScreen < desktop) return 'tablet';
  // return 'desktop';
};

export default getTypeScreen;
